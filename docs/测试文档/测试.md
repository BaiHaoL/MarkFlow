# MarkFlow

Markdown 编辑器，专为 Android 设计。支持 Markdown 及多种文本文件的浏览、编辑、预览与管理，提供流畅的写作体验。

## 功能特性

### 文件管理
- 自动扫描设备上的 Markdown 文件（`.md` / `.markdown`）
- 支持按修改时间或文件名排序
- 文件分区：本地 / 最近打开 / 其他（非 Markdown 文本文件）
- 多选模式：批量删除
- 单文件操作：重命名、查看详情、分享
- 新建 Markdown 文件
- 从手机导入已有文本文档

### 编辑器
- 编辑模式与预览模式切换
- 语法高亮提示（代码块、标题、列表等）
- 撤销 / 重做（含光标位置追踪）
- 自动保存（编辑状态停止输入 3 秒后触发，不影响撤销/重做历史）
- 手动保存，保存状态提示（未保存 / 保存中 / 已保存）
- 退出时未保存更改确认
- 插入图片（自动处理相对路径，复制到 `images/` 目录）
- 编辑模式专属功能：搜索图标、撤销/重做按钮

### 预览
- Markdown 渲染（基于 Markwon）
- 代码块语法高亮（Prism4j，50+ 语言）
- 表格、任务列表、删除线
- 图片加载（Coil）
- 不安全的图片 URL 自动过滤（如 Mi Notes 自定义 URI）
- 亮色 / 暗色主题切换
- Android 12+ 支持 Material You Dynamic Color（跟随系统模式）

### 搜索
- 编辑模式下关键字搜索，匹配结果高亮
- 点击结果精确跳转并居中显示



## 项目结构

```
com.markflow.editor
├── data/
│   ├── local/          # 本地偏好存储（排序模式、主题模式）
│   └── repository/     # 文件 I/O 仓库
├── domain/
│   ├── model/          # 领域模型（EditorMode, FileType, SortMode）
│   └── util/           # 领域工具（排序器, 撤销/重做管理器, 目录解析）
├── ui/
│   ├── components/     # 可复用组件（BottomActionBar, CodeBlockCard, MarkdownPreview, SearchBar）
│   ├── navigation/     # 导航图与路由定义
│   ├── screens/
│   │   ├── editor/     # 编辑器页面 + ViewModel
│   │   └── filelist/   # 文件列表页面 + ViewModel
│   └── theme/          # Material3 主题（含 Dynamic Color 支持）
├── util/               # 工具类（Markwon 配置, 图片路径, 语法高亮）
├── MainActivity.kt     # 主 Activity
└── MarkFlowApp.kt      # Application 入口
```

## 构建

### 环境要求

- Android Studio Hedgehog 或更新版本
- JDK 17
- Android SDK 35
- Gradle 8.7+

### 构建命令

```powershell
# Debug 版本（未混淆，可调试）
.\gradlew.bat assembleDebug

# Release 版本（已混淆、已压缩、已签名）
.\gradlew.bat assembleRelease

# 清理构建产物
.\gradlew.bat clean
```



# MarkFlow

Markdown 编辑器，专为 Android 设计。支持 Markdown 及多种文本文件的浏览、编辑、预览与管理，提供流畅的写作体验。

## 功能特性

### 文件管理
- 自动扫描设备上的 Markdown 文件（`.md` / `.markdown`）
- 支持按修改时间或文件名排序
- 文件分区：本地 / 最近打开 / 其他（非 Markdown 文本文件）
- 多选模式：批量删除
- 单文件操作：重命名、查看详情、分享
- 新建 Markdown 文件
- 从手机导入已有文本文档

### 编辑器
- 编辑模式与预览模式切换
- 语法高亮提示（代码块、标题、列表等）
- 撤销 / 重做（含光标位置追踪）
- 自动保存（编辑状态停止输入 3 秒后触发，不影响撤销/重做历史）
- 手动保存，保存状态提示（未保存 / 保存中 / 已保存）
- 退出时未保存更改确认
- 插入图片（自动处理相对路径，复制到 `images/` 目录）
- 编辑模式专属功能：搜索图标、撤销/重做按钮

### 预览
- Markdown 渲染（基于 Markwon）
- 代码块语法高亮（Prism4j，50+ 语言）
- 表格、任务列表、删除线
- 图片加载（Coil）
- 不安全的图片 URL 自动过滤（如 Mi Notes 自定义 URI）
- 亮色 / 暗色主题切换
- Android 12+ 支持 Material You Dynamic Color（跟随系统模式）

### 搜索
- 编辑模式下关键字搜索，匹配结果高亮
- 点击结果精确跳转并居中显示



## 项目结构

```
com.markflow.editor
├── data/
│   ├── local/          # 本地偏好存储（排序模式、主题模式）
│   └── repository/     # 文件 I/O 仓库
├── domain/
│   ├── model/          # 领域模型（EditorMode, FileType, SortMode）
│   └── util/           # 领域工具（排序器, 撤销/重做管理器, 目录解析）
├── ui/
│   ├── components/     # 可复用组件（BottomActionBar, CodeBlockCard, MarkdownPreview, SearchBar）
│   ├── navigation/     # 导航图与路由定义
│   ├── screens/
│   │   ├── editor/     # 编辑器页面 + ViewModel
│   │   └── filelist/   # 文件列表页面 + ViewModel
│   └── theme/          # Material3 主题（含 Dynamic Color 支持）
├── util/               # 工具类（Markwon 配置, 图片路径, 语法高亮）
├── MainActivity.kt     # 主 Activity
└── MarkFlowApp.kt      # Application 入口
```

## 构建

### 环境要求

- Android Studio Hedgehog 或更新版本
- JDK 17
- Android SDK 35
- Gradle 8.7+

### 构建命令

```powershell
# Debug 版本（未混淆，可调试）
.\gradlew.bat assembleDebug

# Release 版本（已混淆、已压缩、已签名）
.\gradlew.bat assembleRelease

# 清理构建产物
.\gradlew.bat clean
```



# MarkFlow

Markdown 编辑器，专为 Android 设计。支持 Markdown 及多种文本文件的浏览、编辑、预览与管理，提供流畅的写作体验。

## 功能特性

### 文件管理
- 自动扫描设备上的 Markdown 文件（`.md` / `.markdown`）
- 支持按修改时间或文件名排序
- 文件分区：本地 / 最近打开 / 其他（非 Markdown 文本文件）
- 多选模式：批量删除
- 单文件操作：重命名、查看详情、分享
- 新建 Markdown 文件
- 从手机导入已有文本文档

### 编辑器
- 编辑模式与预览模式切换
- 语法高亮提示（代码块、标题、列表等）
- 撤销 / 重做（含光标位置追踪）
- 自动保存（编辑状态停止输入 3 秒后触发，不影响撤销/重做历史）
- 手动保存，保存状态提示（未保存 / 保存中 / 已保存）
- 退出时未保存更改确认
- 插入图片（自动处理相对路径，复制到 `images/` 目录）
- 编辑模式专属功能：搜索图标、撤销/重做按钮

### 预览
- Markdown 渲染（基于 Markwon）
- 代码块语法高亮（Prism4j，50+ 语言）
- 表格、任务列表、删除线
- 图片加载（Coil）
- 不安全的图片 URL 自动过滤（如 Mi Notes 自定义 URI）
- 亮色 / 暗色主题切换
- Android 12+ 支持 Material You Dynamic Color（跟随系统模式）

### 搜索
- 编辑模式下关键字搜索，匹配结果高亮
- 点击结果精确跳转并居中显示



## 项目结构

```
com.markflow.editor
├── data/
│   ├── local/          # 本地偏好存储（排序模式、主题模式）
│   └── repository/     # 文件 I/O 仓库
├── domain/
│   ├── model/          # 领域模型（EditorMode, FileType, SortMode）
│   └── util/           # 领域工具（排序器, 撤销/重做管理器, 目录解析）
├── ui/
│   ├── components/     # 可复用组件（BottomActionBar, CodeBlockCard, MarkdownPreview, SearchBar）
│   ├── navigation/     # 导航图与路由定义
│   ├── screens/
│   │   ├── editor/     # 编辑器页面 + ViewModel
│   │   └── filelist/   # 文件列表页面 + ViewModel
│   └── theme/          # Material3 主题（含 Dynamic Color 支持）
├── util/               # 工具类（Markwon 配置, 图片路径, 语法高亮）
├── MainActivity.kt     # 主 Activity
└── MarkFlowApp.kt      # Application 入口
```

## 构建

### 环境要求

- Android Studio Hedgehog 或更新版本
- JDK 17
- Android SDK 35
- Gradle 8.7+

### 构建命令

```powershell
# Debug 版本（未混淆，可调试）
.\gradlew.bat assembleDebug

# Release 版本（已混淆、已压缩、已签名）
.\gradlew.bat assembleRelease

# 清理构建产物
.\gradlew.bat clean
```

